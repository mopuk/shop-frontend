export type Color = {
  id: number;
  name: string;
  hex_code: string;
  slug: string;
};

export type Material = {
  id: number;
  name: string;
  slug: string;
};

export type Size = {
  id: number;
  name: string;
  sort_order: number;
};
